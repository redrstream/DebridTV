<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark tone the app's gradient starts from (matches BackgroundTop in Theme.kt). -->
    <color name="splash_bg">#0E1013</color>

    <!-- Base theme: Compose drives the actual UI colors; this just gives a
         dark window background and no action bar so there is no white flash. -->
    <style name="Theme.DebridTV" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Launch theme: the branded splash shown while the app cold-starts, then the
         core-splashscreen library hands off to Theme.DebridTV once Compose is up. -->
    <style name="Theme.DebridTV.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.DebridTV</item>
    </style>

    <!-- Fullscreen theme for the player. -->
    <style name="Theme.DebridTV.Player" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
